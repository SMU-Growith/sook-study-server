spring:
  config:
    activate:
      on-profile: local
  datasource:
    url: jdbc:mysql://localhost:3306/sook_study?useSSL=false&serverTimezone=Asia/Seoul
    username: admin
    password: growith1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

jwt:
  secret: test-secret-key-for-jwt-token-generation-minimum-256-bits-required
  time:
    access-token: 3600000
    refresh-token: 10000000
